[package]
name = "cerebrum-embeddings"
version = "0.2.0"
edition = "2021"

[dependencies]
cerebrum-core = { path = "../cerebrum-core" }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rustc-hash = "2.0"

[dev-dependencies]
proptest = "1.5"
